# Each dependency is justified one line in DECISIONS.md (G2). No LangChain/LlamaIndex.
beautifulsoup4==4.12.3   # real HTML parser; strips <style>/<script>/hidden nodes (G: CSS-residue)
lxml==5.3.0              # fast parser backend for BeautifulSoup
rank_bm25==0.2.2         # BM25 lexical retrieval (the sparse half of hybrid)
chromadb==1.5.9          # local persisted vector store; 1.5.x ships cp313 Windows wheels (0.5.x needs a C++ build)
openai==1.59.6           # embeddings only (text-embedding-3-small)
anthropic==0.109.1       # chat LLM (decompose + synthesize); 0.42 lacked output_config (structured JSON)
fastapi==0.138.1         # backend
uvicorn==0.49.0          # ASGI server
python-dotenv==1.2.2     # load .env
pyyaml==6.0.2            # eval/questions.yaml
tiktoken==0.8.0          # token counting for ~800-token chunking
sentence-transformers==5.5.1  # Phase 5 cross-encoder reranker (pulls torch); toggleable via config.USE_RERANKER
yfinance==1.4.1           # free/delayed market data provider for quote/history tools
cachetools==7.1.1         # small TTL cache for market data responses
